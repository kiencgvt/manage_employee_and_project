<li>
  <%= link_to user.employee.profile.name, user %>
  <% if current_user.role.name == 'Admin' && user != current_user%>
    | <%= link_to "Delete", user, method: :delete, data: { confirm: "Are you sure?"} %>
  <% end %>
</li>